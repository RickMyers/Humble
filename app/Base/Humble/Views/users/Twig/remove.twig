{% set perms = user.load() %}
{% if (perms.admin == "Y") %}
    {% if (helper.getSecret() == "Scooby") %}
        {% set x = users.delete() %}
        {% set x = alias.delete() %}
        {% set x = permissions.delete() %}
    {% else %}
        Incorrect response to passphrase question
    {% endif %}
{% else %}
    NOT AUTHORIZED TO BE HERE!
{% endif %}