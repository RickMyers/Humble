{% set results = application.poll() %}
{% set comma   = false %}
{
    {% for id,result in results %}
        {% if (comma) %},{% endif %}
        "{{ id }}": {{ result|json_encode() }}{%set comma = true %}
    {% endfor %}
}

