{
    "note":     "a period of 20 and multiple of 2 would have this section run at a maximum of every 40 seconds.",
    "period":   5,
    "log": {
        "location": "../../logs/cadence.log",
        "max_size": "100K"
    },
    "handlers": {
        "application": {
            "callbacks": {
                "comment": "Put the name of a function callback, which should reside in '/includes/Callbacks.php', and set to true to get it to be executed",
                "yourHandlerHere": false
            },
            "multiple": 4
        }
    }
}
